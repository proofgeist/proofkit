---
import { Code, Tabs, TabItem } from "@astrojs/starlight/components";

interface Props {
  command: string;
  type?: "local" | "execute";
  frame?: Parameters<typeof Code>[0]["frame"];
}

const { command, type = "local", frame = "terminal" } = Astro.props;
---

<Tabs syncKey="packageManager">
  <TabItem label="npm">
    <Code
      code={`${type === "local" ? "npm run" : "npx"} ${command}`}
      lang="bash"
      frame={frame}
    />
  </TabItem>
  <TabItem label="pnpm">
    <Code
      code={`${type === "local" ? "pnpm" : "pnpx"} ${command}`}
      lang="bash"
      frame={frame}
    />
  </TabItem>
  <TabItem label="yarn">
    <Code
      code={`${type === "local" ? "yarn" : "yarn dlx"} ${command}`}
      lang="bash"
      frame={frame}
    />
  </TabItem>
</Tabs>
