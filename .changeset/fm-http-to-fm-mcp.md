---
"@proofkit/fmdapi": minor
"@proofkit/typegen": minor
"@proofkit/cli": minor
"@proofkit/webviewer": patch
---

Rebrand FM HTTP → FM MCP across the stack. The adapter, config fields, and all references now use `fm-mcp` / `FmMcp` naming to reflect the FileMaker MCP server branding.
