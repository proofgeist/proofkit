---
description: 
globs: 
alwaysApply: true
---
This project is a mono repo. Always use PNPM, but never install packages directly to the workspace root unless specifically asked to do so by the user. Instead, change into the specific package you're working on or use the filter flag from PNPM to install the package only where necessary.