Always run `pnpm run ci` from the root of the repo after completing a task and before committing
Always add a changeset file with fixes. Without it, changelog updates and version bumps will not happen.
